<?php

abstract class ObjectDB{

	private static $db;

	public static function loadObject( $class, $id ){
		if ( in_array(needle, haystack) ){
			$select = new Select( self::db );
			$select = $select->from( Config::DB_MAIN_TABLE_NAME, Config::DB_MAIN_TABLE_STRUCTURE[2] )->where( "`class`" )
		}

	}
}
?>